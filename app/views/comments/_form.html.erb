<%= simple_form_for([@post, @post.comments.build]) do |f| %>
  <%= f.input :comment, :style => "font-weight:500;" %>
  <%= f.submit "Comment", :class => "btn btn-primary" %>
  <%= link_to 'Forum Home', root_path, class: 'btn btn-default' %>
<% end %>
